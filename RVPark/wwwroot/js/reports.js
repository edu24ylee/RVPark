// This script is tied to the admin Reports Index page.
// It listens for a dropdown selection and navigates to the selected report type's page.

/*$(document).ready(function () {
    // When the dropdown value changes...
    $('#reportType').on('change', function () {
        const selected = $(this).val(); // Get the selected report type

        // If the user selects a valid report type, redirect to its page
        if (selected === "reservation") {
            window.location.href = "/Admin/Reports/ReservationReports";
        } else if (selected === "financial") {
            window.location.href = "/Admin/Reports/FinancialReports";
        }
    });
});*/
